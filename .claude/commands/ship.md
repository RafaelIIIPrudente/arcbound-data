---
description: Stage, commit (Conventional Commits), and push the current changes.
---

Ship the current work:

1. Show `git status` and `git diff --stat`.
2. Propose a Conventional Commits message derived from the diff.
3. On my confirmation, commit and push to the current branch.

Never commit directly to `main`. If the current branch is `main`, create a
feature branch first.
